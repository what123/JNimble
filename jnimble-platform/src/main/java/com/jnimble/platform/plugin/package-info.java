/**
 * Plugin state persistence and lifecycle management.
 *
 * <p>Provides interfaces and implementations for persisting plugin runtime state
 * (status, installation time, descriptor metadata) and configuring plugin state
 * storage. Supports both the plugin lifecycle and state synchronization with
 * the permission system.</p>
 *
 * <p>插件状态持久化与生命周期管理。提供插件运行时状态的持久化存储接口与实现，
 * 包括状态记录、安装时间、描述符元数据等，并支持插件状态与权限系统的同步。</p>
 */
package com.jnimble.platform.plugin;
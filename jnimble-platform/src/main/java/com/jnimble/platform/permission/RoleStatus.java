package com.jnimble.platform.permission;

/**
 * Enum representing the status of a role.
 */
public enum RoleStatus {

    /** The role is active and its permissions are enforced. */
    ACTIVE,

    /** The role is disabled and its permissions are not enforced. */
    DISABLED
}

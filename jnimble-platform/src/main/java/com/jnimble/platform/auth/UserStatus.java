package com.jnimble.platform.auth;

/**
 * Enum representing the status of a user account.
 */
public enum UserStatus {

    /** The user account is active and can log in. */
    ACTIVE,

    /** The user account is disabled and cannot log in. */
    DISABLED
}

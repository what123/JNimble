/**
 * Core admin controllers for the JNimble admin shell.
 *
 * <p>Contains the home page controller, login endpoint, and shared view model
 * advice that provides common data (languages, current user, site branding)
 * to all admin views.</p>
 *
 * <p>JNimble 管理后台的核心控制器。包含首页控制器、登录端点以及向所有管理视图
 * 提供通用数据（语言、当前用户、站点品牌信息）的共享视图模型增强器。</p>
 */
package com.jnimble.admin.controller;